#Если Сервер Тогда

// Параметры:
//  Ссылка - СправочникСсылка.Товары - 
Процедура Тест(Ссылка) экспорт
	
	ИмяРеквзита = "Поставщик";
	ИменаРеквизитов = Новый Структура;
	ИменаРеквизитов.Вставить("ОсновнойПоставщик", ИмяРеквзита);
	ИменаРеквизитов.Вставить("Код", "Родитель.Код");
	ИменаРеквизитов.Вставить("Ссылка");
	ИменаРеквизитов.Вставить("Ссылка2", "Родитель." + ИмяРеквзита);
	Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, ИменаРеквизитов);
	
КонецПроцедуры

#КонецЕсли
